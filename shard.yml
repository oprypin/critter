name: critter
version: 0.1.0
description:
  Chat bot relaying messages between IRC and Gitter
authors:
  - Oleh Prypin <oleh@pryp.in>
license: MIT

dependencies:
  emoji:
    github: veelenga/emoji.cr

crystal: '>= 0.34.0'

targets:
  critter:
    main: critter.cr
